# Resolve peer deps from each workspace (not root) — prevents React 18/19 conflict between web and mobile
resolve-peers-from-workspace-root=false

# Short virtual store path to avoid Windows MAX_PATH / CMake CMAKE_OBJECT_PATH_MAX issues
virtual-store-dir=C:\pnpm-vs
