{
  "name": "goal-tracker-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "turbo": "latest"
  },
  "packageManager": "pnpm@9.0.0"
}
